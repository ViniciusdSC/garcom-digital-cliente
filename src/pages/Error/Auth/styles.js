import styled from 'styled-components';
import CardNative from '@material-ui/core/Card';
import TypographyNative from '@material-ui/core/Typography';

export const Card = styled(CardNative)`
    margin: 10px;
`;

export const Typography = styled(TypographyNative)`
`